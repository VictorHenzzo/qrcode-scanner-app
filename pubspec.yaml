name: barcode_scanner
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0-272.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.19.1
  cloud_firestore: ^3.2.1
  fake_cloud_firestore: ^1.2.4
  flutter_modular: ^5.0.3
  intl: ^0.17.0
  qr_code_scanner: ^1.0.0
  url_launcher: ^6.1.5
  mobile_scanner: ^2.0.0

dev_dependencies:
  mockito: ^5.2.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/
